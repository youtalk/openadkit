# VisionPilot smoke: file input, non-realtime (run as fast as possible), no loop.
# Data is mounted at /data; speed file is one vehicle-speed float per frame.
source.video_realtime      = false
source.video_loop          = false
source.input_video         = /data/clip.mp4
source.input_vehicle_speed = /data/speed.txt
source.dataset             = open_lane
